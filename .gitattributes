# 统一行尾: 在 Windows 上只要 core.autocrlf 开着, git 会把 shell 脚本 checkout 成 CRLF,
# 而容器里的 shebang 变成 "#!/bin/bash\r" 时会直接报 bad interpreter, wait-for.sh
# 跑不起来 -> inkwell_app 起不来。这里显式钉成 LF, 让不同操作系统拉下来的代码一致。
* text=auto eol=lf
*.sh text eol=lf
*.sql text eol=lf
*.conf text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
Dockerfile text eol=lf
