# 这些文件不需要进构建上下文: 只会拖慢构建、让镜像层的缓存更容易失效。
#
# node_modules 必须排除: 里面有宿主机(可能是 Windows/macOS)装的依赖,
# 拷进去会覆盖容器里 npm ci 装好的那一份, 直接构建失败。
node_modules
dist
.git
.gitignore
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.idea
.vscode
*.sw?
README.md
Dockerfile
.dockerignore

# 本地环境变量文件/日志, 和 .gitignore 保持一致(构建时用的 VITE_API_BASE 走 --build-arg)
.env.local
.env.*.local
*.log
